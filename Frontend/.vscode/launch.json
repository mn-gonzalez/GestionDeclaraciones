{
    "configurations": [
        
        {
            "type": "node-terminal",
            "name": "Run Script: ng serve",
            "request": "launch",
            "command": "ng serve",
            "cwd": "${workspaceFolder}"
        }
    ]
}